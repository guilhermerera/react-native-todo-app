import { StyleSheet } from "react-native";
import { gray600 } from "../../../GlobalStyles";

export const styles = StyleSheet.create({
	bodyContainer: {
		flex: 1,
		justifyContent: "center",
		alignItems: "center",
		backgroundColor: gray600
	},
	mainContainer: {
		flex: 1,
		width: "90%",
		alignItems: "center"
	}
});
